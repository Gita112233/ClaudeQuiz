# CLAUDE.md — บริบทของโปรเจกต์นี้

## นี่คืออะไร

โฟลเดอร์นี้มี quiz web app (`quiz/`) สำหรับติวสอบชีวะเรื่อง **Cell Structure and Functions** (ครบทั้งสไลด์ 147 หน้า) และ **Cell Membrane** (เฉพาะหน้าที่เกี่ยวกับ membrane จริงๆ — หน้า 1–8 ของ `CellMembrane and other.pdf`; หน้าที่เหลือของไฟล์นั้นเป็นเรื่อง Metabolism/Enzyme/Respiration ซึ่ง**ไม่ต้องนำมาทำ quiz** ตามที่ผู้ใช้ระบุ)

ข้อสอบจริงของผู้ใช้: ปรนัย 5 ตัวเลือก 30 ข้อ (ไทย 20/อังกฤษ 10) + จับคู่ 10 ข้อย่อย (อังกฤษ) — **แต่ quiz app นี้ไม่ใช่ข้อสอบจริง** มันคือคลังคำถามฝึกฝนที่ครอบคลุมทุกหัวข้อย่อยในสไลด์ (อย่างน้อย 2 ข้อ/หัวข้อ) เพื่อให้จำเนื้อหาได้ทั้งหมด จึงมีจำนวนข้อเกิน 100 ข้อ (ปัจจุบัน 479 MCQ + 12 ชุดจับคู่ = 88 คู่)

## บทบาทของคุณ (Claude) เมื่อถูกเรียกใช้ในโปรเจกต์นี้ครั้งถัดไป

1. ถ้าผู้ใช้ขอ "เพิ่มเนื้อหาใหม่" หรือมีสไลด์/ไฟล์ PDF ใหม่มาให้ — วิเคราะห์สไลด์ให้ครบทุกหน้า (ห้ามข้าม) แล้วเขียนคำถามตาม schema ด้านล่าง ต่อท้ายไฟล์ source ที่เกี่ยวข้อง (หรือสร้างไฟล์ source ใหม่ถ้าเป็นเนื้อหาหัวข้อใหญ่ใหม่)
2. ถ้าผู้ใช้ขอ "แก้ไข/ลบ คำถามข้อที่ผิด" — หาไฟล์ source ที่มี `id` นั้น (ดูวิธีหา id ด้านล่าง) แล้วแก้ตรงนั้น
3. ถ้าเนื้อหาใหญ่มาก (สไลด์หลายสิบหน้า) — ใช้กลยุทธ์เดิมที่ใช้สร้างโปรเจกต์นี้: แตก agent (Agent tool, general-purpose) ให้อ่านสไลด์เป็นช่วงๆ (ครั้งละ ~20 หน้า) สรุปเนื้อหาละเอียด ส่งกลับมาเป็น text แล้วค่อย spawn agent อีกชุดให้เขียนคำถามจากสรุปนั้น (ไม่ต้องให้ agent เขียนคำถามอ่านสไลด์เองในรอบเดียว เพราะจะไม่ครอบคลุมและคุมสัดส่วนภาษาไม่ได้) — ดู pattern ที่ใช้จริงในบทสนทนาที่สร้างโปรเจกต์นี้เป็นตัวอย่าง (แบ่งงานตาม page range, ส่ง content summary เต็มๆ ให้ agent เขียนคำถาม พร้อม schema + กติกาที่เข้มงวด)
4. เขียนคำถามเสร็จแล้ว **ต้องรัน `node build.js`** เสมอ เพื่อ regenerate `quiz/data/questions.js` — ถ้าลืมขั้นตอนนี้ quiz app จะไม่เห็นคำถามใหม่เลย
5. ทดสอบว่า app ยังทำงานถูกต้องหลังแก้ไข (ดูหัวข้อ "วิธีทดสอบ" ด้านล่าง) ก่อนบอกผู้ใช้ว่าเสร็จแล้ว

## โครงสร้างไฟล์

```
quiz/
  index.html          หน้า quiz (ไม่ต้องแก้บ่อย)
  style.css           สไตล์ (ไม่ต้องแก้บ่อย)
  script.js           logic ของ quiz engine (filter/สุ่ม/เฉลย/hint/matching/สรุปผล)
  build.js            รวมไฟล์ data/source/*.json → data/questions.js (รันทุกครั้งที่แก้คำถาม)
  data/
    questions.js      ไฟล์ที่ index.html โหลดจริง (ห้ามแก้มือ — เป็น output จาก build.js)
    source/
      cs1.json ... cs8.json   คำถาม MCQ จาก Cell Structure and Functions.pdf แบ่งตามช่วงหน้า
                                (cs1=หน้า1-20, cs2=21-40, cs3=41-60, cs4=61-80, cs5=81-100,
                                 cs6=101-120, cs7=121-140, cs8=141-147)
      cm1.json         คำถาม MCQ จาก CellMembrane and other.pdf หน้า 1-8 เท่านั้น
      matching.json    ชุดคำถามจับคู่ (ส่วนที่ 2 ของสไตล์ข้อสอบจริง) ภาษาอังกฤษทั้งหมด
```

**กติกาสำคัญ: ห้ามแก้ `quiz/data/questions.js` ตรงๆ** — มันถูก generate จาก `data/source/*.json` เสมอ แก้ที่ source แล้วรัน `node build.js` แทน

## Schema คำถาม

### MCQ (ไฟล์ cs1.json…cs8.json, cm1.json — เป็น array ของ object เหล่านี้)

```json
{
  "id": "cs1-01",
  "topic": "Cell Theory Timeline",
  "lang": "th",
  "question": "...",
  "choices": ["...", "...", "...", "...", "..."],
  "answerIndex": 0,
  "hint": "...",
  "explanation": "..."
}
```

- `id`: prefix ตามไฟล์ + เลขลำดับ zero-padded (เช่น `cs3-07`, `cm1-12`) ห้ามซ้ำกันข้ามทั้งโปรเจกต์ — `build.js` จะ error ถ้าเจอ id ซ้ำ
- `lang`: `"th"` หรือ `"en"` — สัดส่วนที่ใช้อยู่ตอนนี้ประมาณ 2:1 (ไทย:อังกฤษ) ตามสัดส่วนข้อสอบจริง (20:10)
- `choices`: ต้องมี **5 ตัวเลือกเสมอ** (ไม่ใช่ 4)
- `answerIndex`: 0-4
- `source` จะถูกเติมอัตโนมัติโดย `build.js` (ดูจาก prefix ไฟล์: `cm*` → "Cell Membrane", อื่นๆ → "Cell Structure") **ไม่ต้องใส่เองในไฟล์ source**

### Matching (ไฟล์ matching.json — เป็น array ของ object เหล่านี้)

```json
{
  "id": "match-13",
  "type": "matching",
  "topic": "...",
  "instructions": "Match each ... to its ...",
  "pairs": [{ "left": "...", "right": "..." }, ...],
  "source": "Cell Structure"
}
```

- `pairs`: อย่างน้อย 2 คู่ (ในทางปฏิบัติใช้ 6-10 คู่ต่อชุด)
- `source`: ต้องใส่เอง เป็น `"Cell Structure"` หรือ `"Cell Membrane"` (matching.json ไม่ได้ auto-fill เหมือน MCQ เพราะเนื้อหาผสมได้)
- Matching เป็นภาษาอังกฤษล้วนตามสไตล์ข้อสอบจริง

## กติกาการเขียนคำถามใหม่ (คงไว้เหมือนตอนสร้างโปรเจกต์นี้)

- ครอบคลุมทุกหัวข้อย่อยในเนื้อหา ห้ามข้าม แต่ละหัวข้อย่อยต้องมีอย่างน้อย 2 ข้อ (หัวข้อที่เนื้อหาแน่น/ซับซ้อนควรมี 3-4 ข้อ)
- 1 ข้อ = 1 concept เท่านั้น ห้ามผสมหลายเรื่องในข้อเดียว
- ตัวลวง (choices ที่ผิด) ควรดึงมาจากข้อเท็จจริงอื่นในเนื้อหาเดียวกัน (เช่น สลับชื่อ organelle, สลับตัวเลข, สลับปีที่ค้นพบ) ไม่ใช่ตัวเลือกที่ผิดแบบเดาง่ายเกินไป
- `hint` ต้องช่วยชี้แนวทางแต่ไม่เฉลยตรงๆ
- `explanation` อธิบายสั้นๆ ว่าทำไมข้อที่ถูกต้องจึงถูก
- ห้ามแต่งข้อเท็จจริงที่ไม่มีอยู่ในสไลด์ต้นฉบับ

## วิธีทดสอบหลังแก้ไข

```bash
cd quiz
node build.js                     # ต้องไม่ error, เช็คตัวเลขจำนวนข้อที่ print ออกมาสมเหตุสมผล
node -c data/questions.js         # เช็ค syntax
python3 -m http.server 8080       # เปิด local server (เพราะ index.html โหลดไฟล์ data/questions.js ผ่าน <script src>)
# แล้วเปิด http://localhost:8080 ในเบราว์เซอร์ เพื่อลองทำ quiz จริง 1-2 ข้อ
```

**บั๊กที่เคยเจอและแก้แล้ว (อย่าทำซ้ำ):** ห้ามใช้ `const QUIZ_DATA = {...}` เป็น top-level statement ในไฟล์ `questions.js` — ใน browser, `const`/`let` ที่ top-level ของ `<script>` ธรรมดา (ไม่ใช่ module) จะไม่ผูกเข้ากับ `window` object ทำให้ `window.QUIZ_DATA` เป็น `undefined` และ quiz จะว่างเปล่าโดยไม่มี error ให้เห็นเลย ต้องใช้ `window.QUIZ_DATA = {...}` explicit เสมอ (build.js เขียนแบบนี้อยู่แล้ว อย่าเปลี่ยน)

## ฟีเจอร์ของ quiz app (สำหรับอ้างอิง ไม่ต้องแก้ถ้าไม่มีใครขอ)

- กรองตามเนื้อหา (Cell Structure / Cell Membrane / ทั้งหมด), ประเภทคำถาม (MCQ/Matching/ทั้งหมด), ภาษา, หัวข้อย่อย
- สุ่มลำดับหรือเรียงตามเนื้อหา
- ตอบ MCQ แล้วเฉลยทันที (สีเขียว=ถูก/แดง=ผิด) พร้อมคำอธิบาย
- ปุ่มดู Hint ต่อข้อ (กดได้ก่อนตอบ)
- คำถามจับคู่: คลิกซ้ายแล้วคลิกขวา เฉลยทันทีเป็นสี ต้องจับคู่ให้ครบก่อนไปข้อถัดไป
- แสดงคะแนนสะสมระหว่างทำ + สรุปผลท้ายด้วย breakdown ตามหัวข้อ + ปุ่ม "ทำเฉพาะข้อที่ผิดซ้ำ"
- ทำงาน client-side ล้วนๆ ไม่มีการส่งข้อมูลออกไปไหน
